-- Custom repository for cardano haskell packages, see CONTRIBUTING for more
repository cardano-haskell-packages
  url: https://chap.intersectmbo.org/
  secure: True
  root-keys:
    

-- See CONTRIBUTING for information about these, including some Nix commands
-- you need to run if you change them
index-state:
  , hackage.haskell.org 2026-09-18T01:34:09Z
  , cardano-haskell-packages 2026-09-16T23:53:07Z


constraints:
  -- haskell.nix patch does not work for 1.6.8
  -- grapesy currently depends on crypton-x509-system (>=1.6 && <1.7)
  , any.crypton-x509-system < 1.6.8 || >= 1.7

  -- EXPERIMENT: force cardano-crypto-class up to the 2.6.x line (which
  -- requires crypton ^>=1.1) to see how far the crypton >= 1.1 / memory ->
  -- ram migration gets. Several local packages pin cardano-crypto-class
  -- ^>=2.5, so their bound needs an allow-newer below too.
  , any.cardano-crypto-class ^>=2.6

  -- EXPERIMENT: cardano-crypto-1.2.0 (the version the solver reaches for
  -- otherwise) still uses the old `memory` package for ByteArrayAccess,
  -- which doesn't unify with crypton-1.1.4's `ram`-based Digest instances.
  -- cardano-crypto-1.4.0 (already in CHaP) has switched to ram >=0.22.
  , any.cardano-crypto >= 1.4

  -- haskell.nix tries to use 0.7.1.5 for some reason
  , any.proto-lens >= 0.7.1.7
  -- https://github.com/channable/alfred-margaret/pull/76
  , any.alfred-margaret < 2.1.1.0 || > 2.1.1.0

-- acts has a finitary flag; disabling it avoids pulling in finitary entirely,
-- which sidesteps the GHC type-checker plugin incompatibility with
-- cross-compilation to Windows (finitary-2.2.0.0+)
package acts
  flags: -finitary

packages:
  cardano-node
  cardano-node-capi
  cardano-node-chairman
  cardano-submit-api
  cardano-testnet
  cardano-tracer
  bench/cardano-profile
  bench/cardano-topology
  bench/locli
  bench/plutus-scripts-bench
  bench/tx-generator
  bench/cardano-timeseries-io
  bench/trace-schemas/scripts/schema-gen
  trace-forward

-- Shipped as a release binary but not a dependency of any local package, so it
-- has to be added to the install plan explicitly.
extra-packages: dmq-node >= 0.7.0.0

program-options
  ghc-options: -Werror

test-show-details: direct

-- Always write GHC env files, because they are needed for ghci.
write-ghc-environment-files: always

package cryptonite
  -- Using RDRAND instead of /dev/urandom as an entropy source for key
  -- generation is dubious. Set the flag so we use /dev/urandom by default.
  flags: -support_rdrand

package snap-server
  flags: -openssl

package bitvec
  flags: -simd

package cardano-diffusion
  flags: +optparse-applicative-fork

-- required for haddocks to build successfully
package plutus-scripts-bench
  haddock-options: "--optghc=-fplugin-opt=PlutusTx.Plugin:defer-errors"

-- There is a suspected bug in `cabal` (https://github.com/haskell/cabal/issues/11663)
-- that can be worked around with the following allow-newer stanzas
allow-newer:
  , io-sim:time
  , io-classes:time

  -- EXPERIMENT: these packages pin cardano-crypto-class to <2.6 (either
  -- ^>=2.5 or an explicit >=x.y && <2.6); let them accept the 2.6.x line
  -- so crypton can move to >= 1.1. (cardano-api's own bound is already
  -- ^>=2.6 on the erikd/crypton+ram SRP, cardano-node/-chairman/
  -- cardano-submit-api/cardano-testnet's own bounds were widened to
  -- ^>=2.5||^>=2.6 directly, and ouroboros-consensus's own bound is already
  -- ^>=2.5||^>=2.6 on the erikd/ram SRP, so none of those need an entry
  -- here.)
  , cardano-cli:cardano-crypto-class
  , cardano-keys:cardano-crypto-class
  , cardano-ledger-binary:cardano-crypto-class
  , cardano-ledger-core:cardano-crypto-class
  , cardano-ledger-mary:cardano-crypto-class
  , cardano-ledger-shelley:cardano-crypto-class
  , cardano-protocol:cardano-crypto-class
  , cardano-protocol-tpraos:cardano-crypto-class
  , kes-agent:cardano-crypto-class
  , kes-agent-crypto:cardano-crypto-class

  -- EXPERIMENT: cardano-cli (CHaP, unmodified) pins cardano-crypto-wrapper
  -- ^>=1.7; let it accept the erikd/ram-based 1.8.0.0 SRP above.
  -- (cardano-api's own bound is already ^>=1.7||^>=1.8, no entry needed.)
  , cardano-cli:cardano-crypto-wrapper

  -- EXPERIMENT: cardano-api PR #1292 now depends on the new cardano-keys
  -- package (CHaP 11.0.0.0, extracted from cardano-api's own key-handling
  -- modules -- Ed25519Bip32.hs/Mnemonic.hs/Sign.hs, the actual crypton/ram
  -- fix targets, stayed in cardano-api; cardano-keys took the rest, e.g.
  -- Leios.hs, which is now just a re-export). cardano-keys itself still pins
  -- cardano-crypto-class ^>=2.5 and cardano-crypto-wrapper ^>=1.7 natively
  -- (no memory dependency though, so not a blocker otherwise); let it accept
  -- the widened versions.
  , cardano-keys:cardano-crypto-wrapper

  -- TEMPORARY: cardano-keys (CHaP 11.0.0.0, unmodified) pins
  -- cardano-ledger-core ^>=1.21 and cardano-ledger-shelley ^>=1.19, both
  -- excluding the versions the cardano-ledger monorepo SRP above now
  -- provides (1.22.0.0 / 1.20.0.0); let it accept them.
  , cardano-keys:cardano-ledger-core
  , cardano-keys:cardano-ledger-shelley

-- cabal-allow-newer begin
if impl(ghc >= 9.14)
  allow-newer:
    , async-timer:unliftio-core
    , attoparsec-iso8601:time
    , bytestring-trie:base
    , canonical-json:containers
    , cborg:base
    , cborg:containers
    , cborg-json:aeson
    , cborg-json:base
    , compact:base
    , crypton-connection:tls
    , dependent-map:containers
    , diff-containers:base
    , dictionary-sharing:containers
    , dmq-node:containers
    , dmq-node:time
    , ekg-json:aeson
    , ekg-json:text
    , ekg-prometheus-adapter:containers
    , ekg-wai:time
    , errors:transformers-compat
    , fs-sim:QuickCheck
    , grapesy:aeson
    , grapesy:base
    , grapesy:containers
    , grapesy:time-manager
    , grapesy:tls
    , grpc-spec:aeson
    , grpc-spec:base
    , grpc-spec:containers
    , iohk-monitoring:contra-tracer
    , lens-family:containers
    , lens-family-core:containers
    , locli:ouroboros-network
    , microstache:aeson
    , monad-control:transformers-compat
    , nothunks:containers
    , nothunks:time
    , optparse-generic:time
    , partial-order:containers
    , pipes-safe:containers
    , pipes-safe:mtl
    , pipes-safe:primitive
    , pipes-safe:transformers
    , prometheus:containers
    , safe-wild-cards:template-haskell
    , serdoc-core:template-haskell
    , serialise:base
    , serialise:containers
    , serialise:time
    , servant:QuickCheck
    , servant:base
    , servant-server:aeson
    , servant-server:attoparsec
    , servant-server:base
    , servant-server:base-compat
    , servant-server:base64-bytestring
    , servant-server:bytestring
    , servant-server:containers
    , servant-server:http-api-data
    , servant-server:http-types
    , servant-server:network
    , servant-server:servant
    , servant-server:text
    , servant-server:transformers
    , servant-server:warp
    , strict-checked-vars:io-classes
    , strict-stm:base
    , strict-stm:io-classes
    , tdigest:base
    , time-locale-compat:time
    , tracer-transformers:contra-tracer
    , vary:aeson
    , with-utf8:base
-- cabal-allow-newer end

-- IMPORTANT
-- Do NOT add more source-repository-package stanzas here unless they are strictly
-- temporary! Please read the section in CONTRIBUTING about updating dependencies.

-- cardano-ledger: master tip (b1097d47), kept in sync with
-- ouroboros-consensus's own cardano-ledger SRP below (see that comment) --
-- don't bump this independently of that pin. Pulling in the whole monorepo,
-- not just eras/byron/crypto, because ouroboros-consensus's
-- erikd/update-ledger-srp (below) transitively needs cardano-ledger-binary
-- ^>=1.10, small-steps ^>=1.2 and cardano-ledger-core ^>=1.22 -- none
-- released to CHaP yet -- and every other era package in the monorepo has
-- to move in lockstep with whichever of these it depends on.
source-repository-package
  type: git
  location: https://github.com/IntersectMBO/cardano-ledger.git
  tag: 51756884d0279b185b6c820f681632766ac86579
  --sha256: sha256-Urfo/LRTlGwihogsbNGmVYM/+LcNv1FAMJ0kWMXHZyk=
  subdir:
    eras/allegra/impl
    eras/alonzo/impl
    eras/babbage/impl
    eras/byron/chain/executable-spec
    eras/byron/crypto
    eras/byron/ledger/executable-spec
    eras/byron/ledger/impl
    eras/conway/impl
    eras/dijkstra/impl
    eras/mary/impl
    eras/shelley/impl
    eras/shelley/test-suite
    libs/cardano-data
    libs/cardano-ledger-api
    libs/cardano-ledger-binary
    libs/cardano-ledger-core
    libs/cardano-protocol
    libs/cardano-protocol-tpraos
    libs/non-integral
    libs/small-steps
    libs/vector-map

-- cardano-base (pre-DST-internalisation cardano-crypto-leios): pulled in via
-- ouroboros-consensus's own cardano-ledger SRP above needing
-- cardano-crypto-leios ^>=0.3, which doesn't exist in CHaP. Pinned to
-- 3d5e2ba1 rather than cardano-base's current master tip: the very next
-- commit (58f5a53a) refactors cardano-crypto-class's BLS12-381 DSIGN API in
-- a way cardano-crypto-leios's source (at any commit past that point) is
-- written against, but CHaP's published cardano-crypto-class-2.6.0.0 (what
-- this project resolves cardano-crypto-class from) still has the
-- pre-refactor shape. Matches ouroboros-consensus's own pin for the same
-- reason -- keep them in sync.
source-repository-package
  type: git
  location: https://github.com/intersectmbo/cardano-base.git
  tag: 3d5e2ba1e76b7cd3bead8bd3f81a2e7ee743ce5c
  --sha256: sha256-Wr6CZW4OR4nsbARIbLKIf61jQfO1WsiT9HPLV+mfZYU=
  subdir: cardano-crypto-leios

-- ouroboros-network main: ouroboros-consensus's own cabal.project (see
-- below) itself pins ouroboros-network to this exact commit via its own
-- SRP, rather than CHaP's published ouroboros-network-1.2.0.0. SRPs aren't
-- transitive across cabal.project files, so without this our build would
-- resolve ouroboros-network from CHaP's stale release, causing API
-- mismatches (BlockFetchConsensusInterface/FetchClientRegistry/
-- BlockFetchClient all gained a new `matchedBlock` type parameter).
-- Mirroring ouroboros-consensus's own pin exactly (same tag, same subdirs).
source-repository-package
  type: git
  location: https://github.com/IntersectMBO/ouroboros-network.git
  tag: a3d8017e798b225055aaf9118ad062fe58bc650f
  --sha256: sha256-nrDi43HfvKAg+5wE6PF4QI4HmYh1rav3a5uqw24BFv0=
  subdir:
    ouroboros-network
    cardano-diffusion
    network-mux

-- ouroboros-consensus PR #2313 (erikd/update-ledger-srp):
-- https://github.com/IntersectMBO/ouroboros-consensus/pull/2313
-- Chases cardano-ledger's master tip (see the cardano-ledger SRP above) and
-- fixes what that broke, including the Peras-related
-- DijkstraEraBlockHeader -> LeiosEraBlockHeader rename, the
-- BlockFetchConsensusInterface `matchedBlock` parameter, and the
-- TracePerasCertInclusionEvent/TracePerasVoteForgingEvent tracer additions.
-- Its own .cabal version field was bumped to 4.2.1.0 as part of this branch,
-- satisfying cardano-node's own ^>=4.2.0.1 bound directly -- no allow-older
-- bridge needed. The .cabal lives at the repo root (its own cabal.project
-- says `packages: .`), so no subdir is needed.
source-repository-package
  type: git
  location: https://github.com/IntersectMBO/ouroboros-consensus.git
  tag: 584152d1b021e7cede41b5ef065b0a21924e6fa6
  --sha256: sha256-A/9/O49svOzHRIstJ9SSKpsJceZhui6MocsHLPW793c=
  --sha256: sha256-LD2xbnesyVD8gj3GEIKlVTiWuh0vIEA/FI16q3odH5I=

-- cardano-api PR #1292 (erikd/crypton+ram):
-- https://github.com/IntersectMBO/cardano-api/pull/1292
-- rebased onto current cardano-api master (11.7.0.0), picking up the TCP/TLS
-- RPC listening feature cardano-node master now uses, the
-- ouroboros-consensus ^>=4.2.1.0 bound bump, and the
-- caseShelleyToBabbageOrConwayEraOnwards removal (cardano-cli PR #1417
-- still calls this in 5 places -- needs a matching update, see PR #1417's
-- SRP comment below). Also widens its own resource-registry
-- (^>=0.3||^>=0.4), small-steps (^>=1.1||^>=1.2), cardano-ledger-api
-- (^>=1.14||^>=1.15) and plutus-core/plutus-ledger-api (^>=1.65||^>=1.68)
-- bounds directly, needed now that ouroboros-consensus's
-- erikd/update-ledger-srp SRP above pulls in newer versions of those
-- transitively. cardano-crypto-class ^>=2.6 and cardano-crypto-wrapper
-- ^>=1.7||^>=1.8 still accepted natively, and no `memory` dependency
-- anywhere. NOTE: the previous pin here (81d7655d) is a stale, now-orphaned
-- commit -- the branch was rebased since; 0cc537ee is the current tip.
source-repository-package
  type: git
  location: https://github.com/IntersectMBO/cardano-api.git
  tag: 0cc537ee37d1e35f974d6cd469d332295dd16eb1
  subdir: cardano-api

-- cardano-api PR #1292 (erikd/crypton+ram):
-- https://github.com/IntersectMBO/cardano-api/pull/1292
-- cardano-rpc lives in the same repo/commit as the cardano-api SRP above
-- and needs to move in lockstep with it -- includes the TCP/TLS RPC
-- listening feature, no `memory` dependency. One extra commit on top of the
-- master rebase: cardano-api's own new serverExceptionToClient assignment
-- in Cardano.Rpc.Server was written against an older grapesy
-- (SomeException-typed field); grapesy-1.2.0 (what we resolve) changed this
-- field to ExactException. Fixed by unwrapping via the ExactException
-- pattern before use -- unrelated to crypton/ram, just a side effect of
-- rebasing onto real cardano-api master with a newer Hackage index-state
-- than upstream's own CI uses.
source-repository-package
  type: git
  location: https://github.com/IntersectMBO/cardano-api.git
  tag: 0cc537ee37d1e35f974d6cd469d332295dd16eb1
  subdir: cardano-rpc

-- cardano-cli PR #1417 (erikd/crypton-ram):
-- https://github.com/IntersectMBO/cardano-cli/pull/1417
-- rebased onto current cardano-cli master (11.2.3.1), which now requires
-- cardano-api ^>=11.7 (matching the cardano-api PR #1292 SRP above) and no
-- longer calls the removed caseShelleyToBabbageOrConwayEraOnwards (replaced
-- upstream with inEonForShelleyBasedEra; our own getStakeCred helper in
-- Transaction/Run.hs was renamed to attachWitness by the same upstream
-- commit, logic unchanged). Still pins cardano-crypto-class ^>=2.5 and
-- cardano-crypto-wrapper ^>=1.7 natively (unchanged), so the existing
-- allow-newer entries for those still apply. One extra commit on top: the
-- cardano-api PR #1292 rebase changed cardano-protocol-tpraos's KESPeriod
-- to derive no Integral instance; EraBased.Query.Run's
-- opCertStartingKesPeriod called fromIntegral directly on it -- fixed by
-- unwrapping via unKESPeriod first.
-- Two more commits since: mkStakePoolState (cardano-ledger, above) gained a
-- leading EpochNo parameter, threaded through Type/Common.hs's
-- mkPoolStates and EraBased/Query/Run.hs's writePoolState/
-- runQueryPoolStateCmd; and cardano-api's HasTextEnvelope class now only
-- has one method (textEnvelopeTypes, plural), so cardano-cli's golden tests
-- needed to import the now-standalone textEnvelopeType explicitly.
-- NOTE: the previous pin here (39d02bb1) is a stale, now-orphaned commit --
-- the branch was rebased since; 64317a01 is the current tip.
source-repository-package
  type: git
  location: https://github.com/IntersectMBO/cardano-cli.git
  tag: 64317a0190d7b969e4c605bcb7f0d526d6cb6bd3
  subdir: cardano-cli
