pr: 6446
kind:
  - maintenance
description: |
  - Added `scriv` to nix development shell
  - Set-up `scriv` for tracking changelogs under `cardano-testnet`
  - Added CI action that checks that changelog entries are added when `cardano-testnet` is modified
