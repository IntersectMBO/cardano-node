pr: 6586
kind:
  - maintenance
description: |
  Moved all path functions from `Testnet.Defaults` to `Cardano.Node.Testnet.Paths` in `cardano-node` (committee, DRep, SPO, delegator, and genesis paths).
