### Maintenance

- Removed legacy `iohk-monitoring` tracer configuration keys (`TraceBlockFetchClient`, `TraceForge`, etc.) from `defaultYamlHardforkViaConfig`. These keys were only used by the old tracing backend which has been removed.
